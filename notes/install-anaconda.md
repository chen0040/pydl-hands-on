Anaconda is a must have on windows, after it is installed, you can install other deep learning tools by running commands

```bash
conda install -c conda-forge keras
conda install -c conda-forge tensorflow
```